parameters:
    kernel.secret: 123

mail:
    from: { 'noreply@example.com': 'System' }
    reply_to: { 'real_user@example.com': 'Oleg Gazmanov' }

swiftmailer:
    url: 'null://localhost'
    spool: { type: 'memory' }